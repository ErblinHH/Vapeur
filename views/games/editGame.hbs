<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier un Jeu</title>
    <link rel="stylesheet" href="/editGame.css">
</head>
<body>
    <div id="page">
        <h1 id="title">Modifier le Jeu : {{game.name}}</h1>
        
        <form action="/game/edit/{{game.id}}" method="POST" class="form-container">
            <div class="form-group">
                <label for="name">Nom du Jeu :</label>
                <input type="text" id="name" name="name" value="{{game.name}}" required placeholder="Nom du jeu">
            </div>
            
            <div class="form-group">
                <label for="description">Description :</label>
                <textarea id="description" name="description" required placeholder="Description du jeu">{{game.description}}</textarea>
            </div>
            
            <div class="form-group">
                <label for="releaseDate">Date de Sortie :</label>
                <input type="date" id="releaseDate" name="releaseDate" value="{{game.releaseDate}}" required>
            </div>
            
            <div class="form-group">
                <label for="editorId">Éditeur :</label>
                <select id="editorId" name="editorId">
                    <option value="">-- Aucun éditeur --</option>
                    {{#each editors}}
                        <option value="{{this.id}}" {{#if (eq this.id ../game.editorId)}}selected{{/if}}>{{this.name}}</option>
                    {{/each}}
                </select>
            </div>
            
            <div class="form-group">
                <label for="typeId">Type :</label>
                <select id="typeId" name="typeId" required>
                    {{#each types}}
                        <option value="{{this.id}}" {{#if (eq this.id ../game.typeId)}}selected{{/if}}>{{this.name}}</option>
                    {{/each}}
                </select>
            </div>
            
            <div class="form-actions">
                <button type="submit" class="action-button">Enregistrer les modifications</button>
            </div>
        </form>

        <div class="back-link">
            <a href="/games" class="game-link">Retour à la liste des jeux</a>
        </div>
    </div>
</body>
</html>
